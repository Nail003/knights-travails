export default function smallestKnightMoves(currentPosition, requiredPosition) {
    console.log(currentPosition, requiredPosition);
    const queue = [];
    queue.push(currentPosition);

    while (queue.length > 0) {
        const position = queue.shift();
    }
}
